module.exports = function(socket){/*
var machine=require('./machine')
	,util = require('../util')
	,account = require('./account')
	,dc = require('./dc')
	,machine = require('./machine')
socket.on('email', function (data,next) {next(null)})
	.on('account', function (data,next) {next(data)})
	.on('password', function (data,next) {next(data)})
*/
	
}